# internal\middleware

В данной директории содержится код промежуточных обработчиков (middleware) HTTP-запросов. Они выполняют общие задачи, такие как логирование, аутентификация до того как запрос будет передан целевому обработчику.